services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: gunicorn recharges.wsgi:application --bind 0.0.0.0:8000 --workers 3
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
